update_parent('<%= "#{@table_name}" %>', '<%= "#{attribute_name}"  %>' ,'<%= "#{id}" %>')
jQuery('#session_div').replaceWith("<%= j render(:partial => 'shared/session_div', :object => session_div) %>");
action_select_no_js();
